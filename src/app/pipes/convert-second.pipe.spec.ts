import { ConvertSecondPipe } from './convert-second.pipe';

describe('ConvertSecondPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSecondPipe();
    expect(pipe).toBeTruthy();
  });
});
