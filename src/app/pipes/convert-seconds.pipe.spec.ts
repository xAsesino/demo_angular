import { ConvertSecondsPipe } from './convert-seconds.pipe';

describe('ConvertSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
