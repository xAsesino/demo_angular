import { SamEverywhereDirective } from './sam-everywhere.directive';

describe('SamEverywhereDirective', () => {
  it('should create an instance', () => {
    const directive = new SamEverywhereDirective();
    expect(directive).toBeTruthy();
  });
});
