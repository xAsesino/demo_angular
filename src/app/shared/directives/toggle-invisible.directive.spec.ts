import { ToggleInvisibleDirective } from './toggle-invisible.directive';

describe('ToggleInvisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleInvisibleDirective();
    expect(directive).toBeTruthy();
  });
});
